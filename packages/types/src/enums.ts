export enum AssetDirectionEnum {
    INCOMING,
    OUTGOING
}

export enum TransactionTypeEnum {
    GENERAL,
    CONTRACT,
    ASSET,
    COIN,
    TOKEN,
    NFT
}

export enum TransactionStatusEnum {
    FAILED,
    PENDING,
    CONFIRMED
}
